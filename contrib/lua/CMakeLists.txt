project(lua C)

SET(LIB_NAME lua)

FILE(GLOB_RECURSE HEADER_LIST *.h*)
FILE(GLOB_RECURSE SOURCE_LIST *.c*)

SOURCE_GROUP("lua" FILES ${HEADER_LIST})
SOURCE_GROUP("lua" FILES ${SOURCE_LIST})

ADD_LIBRARY(${LIB_NAME} STATIC ${HEADER_LIST} ${SOURCE_LIST})

SET_TARGET_ATTRIBUTE(lua "contrib")