project(mujs C)

SET(LIB_NAME mujs)

FILE(GLOB_RECURSE HEADER_LIST *.h)
FILE(GLOB_RECURSE SOURCE_LIST *.c)

SOURCE_GROUP("mujs" FILES ${HEADER_LIST})
SOURCE_GROUP("mujs" FILES ${SOURCE_LIST})

ADD_LIBRARY(${LIB_NAME} STATIC ${HEADER_LIST} ${SOURCE_LIST})

SET_TARGET_ATTRIBUTE(mujs "contrib")